# Weather Big Data ETL - Анализ климатических данных

Масштабируемый сервис для анализа климатических данных с использованием микросервисной архитектуры на базе Docker Compose.

## ✨ Особенности

- ✅ **Big Data обработка**: Dask Distributed для параллельной обработки
- ✅ **Хранение данных**: PostgreSQL для структурированного хранения
- ✅ **ETL оркестрация**: Prefect для управления workflow
- ✅ **Визуализация**: Streamlit dashboard с интерактивными графиками
- ✅ **Микросервисы**: Docker Compose оркестрация

## 🏗️ Архитектура

- **Ingestion Service (FastAPI)**: Сбор данных из Open-Meteo API
- **Storage (PostgreSQL)**: Реляционная БД для структурированных данных
- **Data Lake (CSV)**: Файловое хранилище для распределенной обработки
- **Processing (Dask Cluster)**: Распределенная обработка данных
- **Orchestration (Prefect)**: Управление ETL процессами
- **Visualization (Streamlit)**: Интерактивный dashboard

## 🚀 Быстрый старт

### 1. Запуск проекта

```bash
docker-compose up --build
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (или скопируйте из `.env.example`):

```env
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=weather_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DASK_SCHEDULER_ADDRESS=dask-scheduler:8786
```

### 3. Проверка работы

После запуска доступны следующие сервисы:

- **FastAPI Swagger UI**: http://localhost:8000/docs
- **Streamlit Dashboard**: http://localhost:8501 🌡️
- **Dask Dashboard**: http://localhost:8787/status
- **Prefect UI**: http://localhost:4201
- **PostgreSQL**: localhost:5433 (внешний порт, внутри Docker сети: 5432)

### 4. Проверка подключения к PostgreSQL

#### Быстрая проверка через скрипт:
```bash
# Внутри контейнера
docker-compose exec api python check_db.py

# Или локально (если PostgreSQL запущен локально)
python check_db.py
```

#### Проверка через psql:
```bash
docker-compose exec postgres psql -U postgres -d weather_db -c "SELECT version();"
```

Подробная инструкция по проверке БД: [DB_CHECK.md](DB_CHECK.md)

## 📝 Примеры запросов

### Вариант 1: Swagger UI (Рекомендуется)

Откройте http://localhost:8000/docs и используйте интерактивный интерфейс.

### Вариант 2: Python скрипт

```bash
# Полный цикл тестирования
python test_api.py

# Отдельные команды
python test_api.py ingest 2023-01-01 2023-01-31
python test_api.py analyze
python test_api.py clean
```

### Вариант 3: PowerShell (Windows)

```powershell
.\test_curl.ps1
```

### Вариант 4: cURL команды

#### Сбор данных (Ingestion)
```bash
# По умолчанию (2020-2023)
curl -X POST "http://localhost:8000/etl/ingest"

# Кастомный период
curl -X POST "http://localhost:8000/etl/ingest?start_date=2023-01-01&end_date=2023-01-31"
```

#### Анализ данных
```bash
curl -X GET "http://localhost:8000/etl/analyze"
```

#### Очистка данных
```bash
curl -X DELETE "http://localhost:8000/etl/clean"
```

### Вариант 5: HTTP файл (VS Code REST Client)

Используйте файл `test_requests.http` с расширением REST Client в VS Code.

## 📊 Сценарий тестирования

### Шаг 1: "Холодный старт"
Попробуйте запустить `/etl/analyze` без данных.
**Ожидаемый результат**: Ошибка 404 (данные отсутствуют)

### Шаг 2: Ingestion (Сбор данных)
```bash
POST /etl/ingest?start_date=2023-01-01&end_date=2023-01-31
```
**Что происходит**: API скачивает ~744 строки для каждого из 5 городов и сохраняет в `data/`.

### Шаг 3: Distributed Analysis (Dask)
```bash
GET /etl/analyze
```
**Что происходит**: 
- Dask читает все CSV файлы параллельно
- Воркеры обрабатывают данные распределенно
- Возвращается агрегированная статистика по городам

### Шаг 4: Визуализация
Откройте http://localhost:8787/status и запустите `/etl/analyze` несколько раз.
Вы увидите графики параллельной обработки в реальном времени.

## 🛠️ Технологический стек

### Big Data:
- **Dask Distributed** - распределенная обработка данных (Big Data технология)

### Хранение:
- **PostgreSQL** - реляционная база данных

### ETL Оркестрация:
- **Prefect** - workflow orchestration

### Визуализация:
- **Streamlit** - интерактивный dashboard
- **Plotly** - интерактивные графики

### Backend:
- **Python 3.11**
- **FastAPI** - REST API
- **Pandas** - обработка данных
- **SQLAlchemy** - ORM для PostgreSQL

### Инфраструктура:
- **Docker Compose** - оркестрация микросервисов

## 📁 Структура проекта

```
weather_bigdata/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI сервис
│   ├── database.py          # PostgreSQL модели и функции
│   ├── prefect_flow.py      # Prefect ETL workflow
│   └── streamlit_app.py     # Streamlit dashboard
├── data/                    # CSV файлы (автоматически)
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── ARCHITECTURE.md          # Документация архитектуры
├── README.md
├── DB_CHECK.md             # Инструкция по проверке PostgreSQL
├── check_db.py             # Скрипт для проверки подключения к БД
├── test_api.py             # Python скрипт для тестирования
├── test_curl.ps1           # PowerShell скрипт
├── test_curl.sh            # Bash скрипт
└── test_requests.http      # HTTP файл для REST Client
```

## 🎯 Особенности

- ✅ **Микросервисная архитектура**: Разделение ответственности
- ✅ **Big Data обработка**: Dask для распределенных вычислений
- ✅ **Структурированное хранение**: PostgreSQL с индексами
- ✅ **ETL оркестрация**: Prefect для управления workflow
- ✅ **Data Lake паттерн**: CSV Partitioning для параллельной обработки
- ✅ **Интерактивная визуализация**: Streamlit с Plotly графиками
- ✅ **Масштабируемость**: Легко добавить больше Dask workers
- ✅ **Мониторинг**: Dask Dashboard, Prefect UI

## 📊 Использование Streamlit Dashboard

1. Откройте http://localhost:8501
2. Используйте фильтры в боковой панели:
   - Выбор города
   - Количество записей
3. Просматривайте вкладки:
   - **Графики**: Временные ряды температуры и влажности
   - **По городам**: Сравнение метрик между городами
   - **Агрегированные данные**: Статистика из PostgreSQL
   - **Сырые данные**: Таблица с возможностью сортировки и экспорта

## 🔄 Использование Prefect

Prefect flow автоматически запускается при старте сервиса. Для ручного запуска:

```python
from app.prefect_flow import weather_etl_flow

# Запуск ETL процесса
result = weather_etl_flow("2023-01-01", "2023-12-31")
```

Или через Prefect UI: http://localhost:4200

## 📈 Масштабирование

Для увеличения количества воркеров:

```bash
docker-compose up --scale dask-worker=4
```

Или измените `replicas` в `docker-compose.yml` (требует Docker Swarm).

## 🔍 Что демонстрирует проект

- **ETL пайплайн**: Extract (Open-Meteo API) → Transform (Dask) → Load (PostgreSQL + CSV)
- **Big Data технология**: Dask для распределенной обработки данных
- **Система хранения**: PostgreSQL для структурированных данных
- **ETL оркестрация**: Prefect для управления workflow
- **Визуализация**: Streamlit dashboard с интерактивными графиками
- **Микросервисы**: Разделение ответственности между компонентами
- **Docker**: Контейнеризация и оркестрация сервисов

## 📚 Документация

Подробная документация архитектуры доступна в [ARCHITECTURE.md](ARCHITECTURE.md):
- Описание компонентов системы
- Data flow диаграммы
- Этапы обработки данных
- Best practices

